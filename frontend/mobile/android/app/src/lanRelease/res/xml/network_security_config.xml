<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Explicit LAN release variant; application code still rejects public HTTP hosts. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
