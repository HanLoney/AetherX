{
  "name": "aetherx-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Independently deployable backend for AetherX",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
