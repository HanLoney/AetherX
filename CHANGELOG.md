# 变更记录

本文件记录 AetherX 面向使用者和部署者的重要变化。所有可发布组件从仓库根目录 `VERSION` 读取统一语义化版本。

## [Unreleased]

## [1.2.9]

### 新增

- 桌面端支持由当前活动手机 Hub 扫码授权登录，并在 Peer 凭据失效时通过 Space Key 证明安全轮换凭据；
- 电脑端与 Android Local Hub 增加局域网广播、邻居探测和身份验证，手机 Hub 在线时可自动发现并接入；
- 登录页直接展示电脑 Hub、手机 Hub 与当前连接状态，扫码登录和账号登录集中在同一界面。

### 修复

- 修复手机 Hub 活动时桌面端无法自动搜索、连接或完成扫码登录的问题；
- 修复旧 Peer 凭据导致 `PEER_AUTH_INVALID`、`DESKTOP_LOGIN_MOBILE_HUB_REQUIRED` 和增量同步中断的问题；
- 修复主动提醒可能创建额外会话的问题，所有聊天与主动消息继续归入唯一主会话；
- 完善动态端点回写、认证握手和同步重试，避免双 Hub 地址变化后长期停留在异常状态。

### 优化

- 移除桌面登录页无操作价值的宣传区，放大二维码并压缩布局，降低滚动和低分辨率识别问题。

## [1.2.8]

### 修复

- 修复手机 Hub 活动时局域网地址发现错误改写主客户端路由，导致聊天发送后切回电脑 Hub 和界面反复刷新的问题；
- 手机 Hub 聊天记录改为分段渲染，避免长历史对话一次性创建大量节点造成头像和气泡跳动；
- 完善双 Hub 动态端点传播、后台增量追平和原生同步错误状态，提升地址变化及应用退到后台后的恢复能力；
- 区分手机客户端连接通道与双 Hub 复制状态，避免数据已经同步时仍显示连接异常。

## [1.2.7]

### 修复

- 修复手机端同步成功时连接状态仍显示异常，以及持续刷新造成的页面抖动；
- 完善电脑 Hub 与手机 Hub 的一体化配对、自动地址探测和断线恢复闭环；
- 修复干净环境中 Electron 运行时未下载，导致 Windows 安装包构建失败的问题。

## [1.2.6]

### 新增

- Vue + Capacitor Android 客户端；
- 短时设备配对、可撤销设备令牌、增量变化日志和 SSE 通知；
- 一体化配对短码，一次完成手机客户端登录与 Android Local Hub 建立，并自动检测 USB、局域网和 Anywhere 可达入口；
- 电脑 Hub 与手机 Hub 的签名复制、安全切换、同步进度和分叉恢复闭环；
- 桌面端内置 Hub 生命周期与启动器 Hub 无损接管，并在连接中心统一展示双 Hub、客户端和网络状态；
- 已撤销设备记录的独立删除入口；
- 多账号登录与基于服务端认证上下文的数据隔离；
- 自托管、开发、备份、安全和贡献文档。

### 安全

- Android 会话令牌使用系统 Keystore；
- 桌面端使用 Electron `safeStorage` 保存会话；
- AI Provider 凭证使用 Hub 主密钥加密；
- 远程移动端连接默认要求 HTTPS，本地调试仅放行回环地址。
- 桌面端和启动器升级到 Electron 43，并把完整依赖审计纳入发布门禁。

### 注意

- 双 Hub 分叉恢复当前按完整权威分支选择，尚不支持逐实体或字段级合并；
- 手机 Hub 独立活动且电脑完全离线时，暂不支持兼容 `.aetherx` 完整存档导出与恢复；
- 项目早期历史版本没有完整的公开变更记录，避免在这里补写未经验证的发布日期和内容。
