# 变更记录

本文件记录 AetherX 面向使用者和部署者的重要变化。所有可发布组件从仓库根目录 `VERSION` 读取统一语义化版本。

## [Unreleased]

## [1.2.6]

### 新增

- Vue + Capacitor Android 客户端；
- 短时设备配对、可撤销设备令牌、增量变化日志和 SSE 通知；
- 一体化配对短码，一次完成手机客户端登录与 Android Local Hub 建立，并自动检测 USB、局域网和 Anywhere 可达入口；
- 电脑 Hub 与手机 Hub 的签名复制、安全切换、同步进度和分叉恢复闭环；
- 桌面端内置 Hub 生命周期与启动器 Hub 无损接管，并在连接中心统一展示双 Hub、客户端和网络状态；
- 已撤销设备记录的独立删除入口；
- 多账号登录与基于服务端认证上下文的数据隔离；
- 自托管、开发、备份、安全和贡献文档。

### 安全

- Android 会话令牌使用系统 Keystore；
- 桌面端使用 Electron `safeStorage` 保存会话；
- AI Provider 凭证使用 Hub 主密钥加密；
- 远程移动端连接默认要求 HTTPS，本地调试仅放行回环地址。
- 桌面端和启动器升级到 Electron 43，并把完整依赖审计纳入发布门禁。

### 注意

- 双 Hub 分叉恢复当前按完整权威分支选择，尚不支持逐实体或字段级合并；
- 手机 Hub 独立活动且电脑完全离线时，暂不支持兼容 `.aetherx` 完整存档导出与恢复；
- 项目早期历史版本没有完整的公开变更记录，避免在这里补写未经验证的发布日期和内容。
